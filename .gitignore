# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Node
node_modules/
dist/
.angular/

# Generated by `python -m app.normalize` — reproducible, but committed so the repo
# can be browsed without running anything first. Ratings are runtime state.
data/ratings.json
