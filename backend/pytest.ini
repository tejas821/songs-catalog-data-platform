[pytest]
testpaths = tests

# Put the backend/ directory on sys.path so `from app.store import ...` resolves.
# Without this, bare `pytest` fails with ModuleNotFoundError while `python -m pytest`
# succeeds — `python -m` prepends the cwd, the console script does not. Both work now.
pythonpath = .
